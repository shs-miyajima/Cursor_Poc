# 設計レビューチェックリスト（AI 独立レビュー）— アンケート作成・回答・管理システム

> 目的: 設計の見落としは下流（テスト設計・実装）でのやり直しコストが最も大きい。
> 作成コンテキストを引き継がない**専用レビューサブエージェント**（`sdd-design-reviewer`、readonly）が、
> このチェックリストに沿って別視点で検証する。影響範囲（§5 `IMPACT-xx`）は記載の突合だけでなく、
> **実際のコードベースを検索して裏取り**する。
>
> 運用: `.cursor/rules/sdd-workflow.mdc`「フェーズ 2 > 独立レビュー（承認確認前に必須）」を参照。
> このレビューは人間の承認を代替しない（自己承認の禁止の原則は変わらない）。

## 使い方

1. `02-design.md` の作成後、**承認確認を人間に提示する前**に、専用レビューサブエージェント
   `sdd-design-reviewer`（定義: `.cursor/agents/sdd-design-reviewer.md`、readonly）へ
   `01-requirements.md` / `02-design.md` / 本チェックリストのパスを渡し、
   下記項目を ✓ / ✕ と理由で判定させる
2. ✕ の指摘のうち機械的に自動修正可能なもの（記載漏れ・ID 付与漏れ・「影響なし」の理由未記入等）は
   メインの AI がその場で修正する
3. 設計方針の変更・要件の解釈など判断が必要な指摘は自動修正せず、承認確認の提示に
   含めて人間に判断を仰ぐ
4. 判定結果と対応状況を「4. 総合判定」の実施記録に記入する

## 1. 要件カバレッジ

- [x] `01-requirements.md` の全ユースケース（§4 UC-xx）が、設計のどのクラス・画面で実現されるか特定できるか
      （初回 ✕: UC-10 の SU 企業選択が S-07 設計に未記載 → §2.1 / §2.4 に追記して解消）
- [x] バリデーション（§7）の全条件に対応する検証箇所（Form Request 等）が設計に含まれているか
- [x] 全画面項目（§5.2）が Blade / JS の設計に含まれているか
      （初回 ✕: 性別未選択時の「未回答」デフォルトの扱いが未記載 → §2.4 / §2.6 に追記して解消）
- [x] 権限（§8）の制御箇所（ルート・ミドルウェア・画面出し分け）が設計に明記されているか
- [x] 非機能要件（§9）の実現方法が設計に記載されているか
      （初回 ✕: NFR-01（bcrypt）・NFR-07（日本語文言）の実現方法が未記載 → §1 に追記して解消）
- [x] Laravel / フロント（Blade / Vite / JS）の責務分界が `01-requirements.md` の記載と一致しているか

## 2. 影響範囲（§5 IMPACT-xx）— コードベースとの突合

- [x] 変更対象のテーブル・モデルを参照する既存の画面・API・Service が `IMPACT-xx` として列挙されているか
      （レビュアーが app/ resources/ routes/ database/ config/ を Grep し、設計に載っていない参照箇所なしを確認。
      `config/auth.php` の User 参照は IMPACT-02 に記載済み・変更不要）
- [x] 共通部品（レイアウト・共通 JS・共有 Service）への波及が検討されているか（IMPACT-05/06/09）
- [x] 「影響なし」とした項目に、検討した上での判断であることが分かる理由が書かれているか（IMPACT-04/08/09/11/12/13）
- [x] 既存 E2E（`tests/e2e_tests/tests/`）のセレクタ・データ前提への影響が検討されているか
      （test_example.spec.ts はタイトル検証のみ → 影響なし = IMPACT-08）
- [x] Migration が既存データ・既存テーブルに与える影響（NOT NULL 追加・型変更等）が検討されているか
      （users の email unique 変更・gender default = IMPACT-01。既存データはローカル seeder のみ）

## 3. 設計品質

- [x] Controller は薄く、ビジネスロジックが Service に置かれているか
- [x] クラス名・メソッド名が命名規約（PSR-4 / `StudlyCase` / `camelCase`）に沿っているか
- [x] メソッド概要がシグネチャレベルで書かれ、人間がレビューできる粒度か（実装コードなし）
- [x] Migration の内容（カラム・型・制約・インデックス）が要件と矛盾していないか
      （初回 ✕: gender カラムと Gender enum の対応が不明確 → §2.6 に保存形式・default を明記して解消。
      その後の人間レビュー差戻しで gender は key（英語コード）保存に変更し、インデックス設計一覧を §2.6 に追加）
- [x] 画面・ルート・フロント（Blade / JS）の構成が過不足なく記載されているか
      （低指摘: ナビメニュー構成・UC-29 の終了表示・S-09/S-10 の 404 方針・NFR-06 の集計方式を §3.1 / §6 に追記して解消）

## 4. 総合判定

| 状態 | 条件 | 対応 |
|------|------|------|
| 指摘なし | 1〜3 のすべての項目が ✓ | そのまま承認確認を人間に提示する |
| 自動修正済み | ✕ はあったが機械的に修正できた | 修正内容を記載の上、承認確認を人間に提示する |
| 要判断 | 設計方針の変更等、自動修正できない ✕ が残る | 承認確認に残課題として明記し、人間に判断を仰ぐ（自己判断で解消したことにしない） |

<!-- レビュー実施記録 -->

| 実施日 | レビュー方式 | 判定 | 指摘事項と対応 |
|--------|-------------|------|----------------|
| 2026-07-08 | 独立サブエージェント（sdd-design-reviewer、readonly・新規セッション） | 自動修正済み | 判定 ✓12 / ✕4、指摘 7 件（中 3・低 4）。すべて記載漏れ等の機械修正可: (1) UC-10 の SU 企業選択 → §2.1/§2.4 追記 (2) 性別「未回答」デフォルト → §2.4/§2.6 追記 (3) NFR-01/07 実現方法 → §1 追記 (4) gender migration と Gender enum の整合 → §2.6 明記 (5) ナビメニュー構成 → §3.1 追記 (6) UC-29 終了表示・回答者側 404 方針 → §3.1/§6 追記 (7) NFR-06 集計方式 → §6 追記。要判断事項なし。影響範囲はコードベース Grep で裏取り済み（設計外の参照なし） |
| 2026-07-08 | 人間レビュー（差戻し対応） | 修正済み | 差戻し指摘 2 件: (1) gender の DB 保存値を日本語 value から key（male/female/other/no_answer）に変更（§2.3/§2.4/§2.6） (2) インデックス設計一覧（全テーブル・15 件 + 張らない判断の理由）を §2.6 に追加 |
