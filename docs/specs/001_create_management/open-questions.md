# 未解決の質問

フェーズ 1 でユーザーへの質問を記載。解決したら回答を追記し、`01-requirements.md` に反映する。

| # | 質問 | 回答 | 状態 |
|---|------|------|------|
| 1 | 外部仕様では新規申請フォームに「理由」を入力するとありますが、`equipment_loan_requests` の DB 定義に理由カラムがありません。理由は保存対象にしますか。保存する場合、カラム名は `reason`、型は text、必須/任意はどちらにしますか。 | 保存する。カラム名は `reason`、型は text、任意。 | closed |
| 2 | 備品の `stock_count` は定義されていますが、在庫数を超える同一備品の同時貸出を禁止する要件は明記されていません。今回の PoC では在庫数チェックを実装対象にしますか。 | 実装対象にする。同一備品の貸出中数が `stock_count` 以上なら申請不可。 | closed |
| 3 | 管理者（`role = admin`）はステータス更新が可能とされていますが、新規申請も可能にしますか。それとも新規申請は一般社員のみとしますか。 | 管理者も利用者として新規申請可能。 | closed |
| 4 | 擬似ログインの初期ユーザーは固定しますか。例: 最初の管理者、最初の一般社員、または未選択から開始。 | 最初の一般社員を初期選択する。 | closed |
| 5 | 画面 URL は何にしますか。例: `/equipment-loans`、`/management/equipment-loans`。 | `/equipment-loans` とする。 | closed |
| 6 | API エラー時の HTTP ステータスについて、存在しない `mock_user_id` / `equipment_id` は 404 と 422 のどちらに統一しますか。 | 存在しない ID は 404、未指定は 422 とする。 | closed |
| 7 | 日付の境界条件として、貸出開始日と貸出終了日が同日の申請は許可しますか。 | 許可する。 | closed |
| 8 | 貸出開始日に過去日を指定する申請は許可しますか。 | 許可しない。 | closed |
| 9 | ステータス遷移の制約は設けますか。例: `returned` から `approved` へ戻せない、`rejected` から `returned` にできない等。 | 設けない。管理者は許可ステータスへ直接更新可能。 | closed |
| 10 | 初期データとして、ユーザー・備品を Seeder で投入しますか。投入する場合、必要なユーザー名・部署・備品名の具体値はありますか。 | サンプルユーザー・備品を Seeder で投入する。具体値はフェーズ 2 で設計する。 | closed |
